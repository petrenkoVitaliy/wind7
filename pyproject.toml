[tool.pyright]
typeCheckingMode = "standard"
exclude = ["playground/**"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "B", "SIM", "ARG", "RUF"]
ignore = ["SIM108"]
per-file-ignores = {"playground/*" = ["ALL"]}